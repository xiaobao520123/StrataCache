# StrataCache tests (no pytest)

These tests are intentionally simple `assert`-based scripts, runnable without pytest.

Run:

```bash
PYTHONPATH=/home/mh/LMCache/stratacache/src python /home/mh/LMCache/stratacache/tests/run_all.py
```

